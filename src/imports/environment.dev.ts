import { Environment } from './environment.i';

export const environment: Environment = {
    production: false,
    supportedLanguages: ['en', 'fr', 'tr'],
    ecommerceHost: 'http://localhost/api',
    identityHost: 'http://localhost/identity'
};

